// @ts-nocheck
// PostAll Auth Configuration
// Uses Supabase Auth directly (magic link via phone/email)

// Re-export Supabase auth helpers for convenience
export { createClient } from '@/lib/supabase/server';
