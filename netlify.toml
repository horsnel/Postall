[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-detects Next.js and adds @netlify/plugin-nextjs
# No need to specify it here to avoid duplication
